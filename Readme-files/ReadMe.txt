Temperature Feedback Model ReadMe:

1. When ran, the code will prompt the user to input a power level. Input the desired power as an integer, in units of MWth.

2. The code contains an If statememt, based on several options for variable assumptions. Currently, the code is set to default to Option 1.

3. The code will output three k values, but other values like the core dimensions are available in the variable explorer.